# Habbit

Habit tracker (prod-ready) for habbit.stanodn.org.

## Setup
- Copy `.env.example` to `.env` and fill secrets.
- Run `docker-compose up -d`.

## SMTP
Uses mailu SMTP at mail.stanodn.org:465.
